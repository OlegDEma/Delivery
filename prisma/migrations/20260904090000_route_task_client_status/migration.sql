-- ТЗ docx 03.09.26: у другому рядку запису адреси показуємо СТАТУС клієнта
-- (відправник / отримувач / пасажир). Для ручних адрес він досі ніде не
-- зберігався — використовувався лише для визначення напрямку і губився.
ALTER TABLE "route_tasks" ADD COLUMN IF NOT EXISTS "manual_client_status" TEXT;
