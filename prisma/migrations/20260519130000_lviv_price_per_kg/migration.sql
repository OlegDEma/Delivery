-- Per ТЗ §49/§50: «якщо у вкладці "Отримувач" вказано населений пункт
-- Львів — величина оплати становить 1,5 €/кг (NL) / 1,0 €/кг (AT)».
-- Знижена ціна за кг для посилок, чий Отримувач у Львові. NULL = винятку
-- немає, діє звичайна price_per_kg.
ALTER TABLE "pricing_config"
  ADD COLUMN IF NOT EXISTS "lviv_price_per_kg" NUMERIC(10, 2);
